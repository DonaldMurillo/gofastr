# Archived prompt treatments

The markdown files in this directory are retained only to explain historical
evaluation artifacts. They injected evaluator-authored design direction into
candidate projects and therefore cannot measure GoFastr's natural behavior.

The active `suite.json` does not reference or copy these files. New evaluations
must vary `framework_root` snapshots while keeping the neutral task and builder
prompt fixed.
