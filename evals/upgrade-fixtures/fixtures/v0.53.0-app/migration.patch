diff --git a/entities/tags.go b/entities/tags.go
index e74de3f..00a5b53 100644
--- a/entities/tags.go
+++ b/entities/tags.go
@@ -255,8 +255,7 @@ func registerTags(app *framework.App) {
 		Fields: []schema.Field{
 			{Name: "name", Type: schema.String, Required: true, Unique: true},
 		},
-		Public:     true,
-		CRUD:       boolPtr(true),
+		Exposure:  &framework.ExposureConfig{CRUD: boolPtr(true), Public: true},
 		Properties: map[string]any{"icon": "tag", "label": "Tags"},
 	})
 	_ = Tags{}
diff --git a/entities/tasks.go b/entities/tasks.go
index 085cb44..9bc50d8 100644
--- a/entities/tasks.go
+++ b/entities/tasks.go
@@ -270,9 +270,8 @@ func registerTasks(app *framework.App) {
 		Relations: []framework.Relation{
 			{Type: framework.RelManyToOne, Name: "owner", Entity: "users", ForeignKey: "user_id"},
 		},
-		OwnerField: "user_id",
-		CRUD:       boolPtr(true),
-		MCP:        true,
+		Scope:     &framework.ScopeConfig{OwnerField: "user_id"},
+		Exposure:  &framework.ExposureConfig{CRUD: boolPtr(true), MCP: true},
 		Properties: map[string]any{"icon": "check-square", "label": "Tasks"},
 	})
 	_ = Tasks{}
diff --git a/entities/users.go b/entities/users.go
index 05d25e6..f7454d9 100644
--- a/entities/users.go
+++ b/entities/users.go
@@ -258,8 +258,10 @@ func registerUsers(app *framework.App) {
 			{Name: "name", Type: schema.String, Required: true},
 			{Name: "email", Type: schema.String, Required: true, Unique: true},
 		},
-		Access:     framework.AccessControl{Read: "users:read", Create: "users:write", Update: "users:write", Delete: "users:admin"},
-		CRUD:       boolPtr(true),
+		Exposure:  &framework.ExposureConfig{
+			CRUD: boolPtr(true),
+			Access: framework.AccessControl{Read: "users:read", Create: "users:write", Update: "users:write", Delete: "users:admin"},
+		},
 		Properties: map[string]any{"icon": "user", "label": "Users"},
 	})
 	_ = Users{}
diff --git a/main.go b/main.go
index 4181027..2499058 100644
--- a/main.go
+++ b/main.go
@@ -17,7 +17,7 @@ import (
 	fwimage "github.com/DonaldMurillo/gofastr/framework/image"
 	"github.com/DonaldMurillo/gofastr/framework/isolation"
 	"github.com/DonaldMurillo/gofastr/framework/uihost"
-	_ "github.com/mattn/go-sqlite3"
+	_ "github.com/DonaldMurillo/gofastr/sqlite/stdlib"
 
 	"example.com/upgrade-fixture/entities"
 )
diff --git a/resource.go b/resource.go
index cb327a5..e9ecb72 100644
--- a/resource.go
+++ b/resource.go
@@ -18,6 +18,7 @@ import (
 	"github.com/DonaldMurillo/gofastr/core/render"
 	"github.com/DonaldMurillo/gofastr/framework"
 	"github.com/DonaldMurillo/gofastr/framework/filter"
+	"github.com/DonaldMurillo/gofastr/framework/headless"
 	"github.com/DonaldMurillo/gofastr/framework/ui"
 )
 
@@ -538,7 +539,13 @@ func (c ResourceConfig) Form(ctx context.Context, id string) render.HTML {
 			cur = resCell(resGet(row, f.Key))
 		}
 		fields = append(fields, ui.FormField(ui.FormFieldConfig{
-			Label: f.Label, For: "f-" + f.Key, Input: c.formInput(ctx, f, cur, rel),
+			Label: f.Label, For: "f-" + f.Key,
+			// v0.86.0: Input is a builder. The field hands the control
+			// its id, its aria-describedby and its invalid state before
+			// the control renders, instead of splicing them in after.
+			Input: func(fc headless.FieldControl) render.HTML {
+				return c.formInput(ctx, fc, f, cur, rel)
+			},
 		}))
 	}
 	form := ui.Form(ui.FormConfig{Action: rpc, Method: "POST", SubmitLabel: submit, ExtraAttrs: attrs, Ctx: ctx}, fields...)
@@ -551,14 +558,26 @@ func (c ResourceConfig) Form(ctx context.Context, id string) render.HTML {
 // formInput builds the typed control for one field, prefilled with cur. Enums
 // and relations render their options server-side; relations resolve to the same
 // human label the list/detail show.
-func (c ResourceConfig) formInput(ctx context.Context, f ResField, cur string, rel map[string]map[string]string) render.HTML {
-	id := "f-" + f.Key
+func (c ResourceConfig) formInput(ctx context.Context, fc headless.FieldControl, f ResField, cur string, rel map[string]map[string]string) render.HTML {
+	// The id and the description come from the field now: one source
+	// for both halves of the label/control relationship.
+	id := fc.ID
+	wired := html.Attrs{}
+	if fc.DescribedBy != "" {
+		wired["aria-describedby"] = fc.DescribedBy
+	}
+	if fc.Invalid {
+		wired["aria-invalid"] = "true"
+	}
+	if fc.Required {
+		wired["required"] = "required"
+	}
 	if labels, ok := rel[f.Key]; ok {
 		opts := []html.SelectOption{{Value: "", Text: "— Select —"}}
 		for val, label := range labels {
 			opts = append(opts, html.SelectOption{Value: val, Text: label, Selected: val == cur})
 		}
-		return html.Select(html.SelectConfig{Name: f.Key, ID: id, Options: opts})
+		return html.Select(html.SelectConfig{Name: f.Key, ID: id, Options: opts, ExtraAttrs: wired})
 	}
 	switch f.Type {
 	case "enum":
@@ -566,17 +585,20 @@ func (c ResourceConfig) formInput(ctx context.Context, f ResField, cur string, r
 		for _, v := range f.Values {
 			opts = append(opts, html.SelectOption{Value: v, Text: resTitle(v), Selected: v == cur})
 		}
-		return html.Select(html.SelectConfig{Name: f.Key, ID: id, Options: opts})
+		return html.Select(html.SelectConfig{Name: f.Key, ID: id, Options: opts, ExtraAttrs: wired})
 	case "text":
-		return html.TextArea(html.TextAreaConfig{Name: f.Key, ID: id, Content: cur, Rows: 4})
+		return html.TextArea(html.TextAreaConfig{Name: f.Key, ID: id, Content: cur, Rows: 4, ExtraAttrs: wired})
 	case "bool", "boolean":
 		attrs := html.Attrs{}
+		for k, v := range wired {
+			attrs[k] = v
+		}
 		if resTruthy(cur) {
 			attrs["checked"] = "checked"
 		}
 		return html.Input(html.InputConfig{Type: "checkbox", Name: f.Key, ID: id, ExtraAttrs: attrs})
 	default:
-		return html.Input(html.InputConfig{Type: resInputType(f.Type), Name: f.Key, ID: id, Value: cur})
+		return html.Input(html.InputConfig{Type: resInputType(f.Type), Name: f.Key, ID: id, Value: cur, ExtraAttrs: wired})
 	}
 }
 
